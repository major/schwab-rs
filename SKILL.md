# schwab-agent skill

Use [`src/bin/schwab-agent/SKILL.md`](src/bin/schwab-agent/SKILL.md) for the full LLM-facing command contract, workflows, JSON output shapes, and safety rules for the bundled `schwab-agent` CLI.

This root pointer keeps the agent guide discoverable from repository-level browsing while the detailed CLI guide stays next to the binary source it describes.
